// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("HyperMobile", "for HyperMobile support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("HyperMobile", "enable HyperMobile support", "no");

if (PHP_HYPERMOBILE != "no") {
	EXTENSION("HyperMobile", "HyperMobile.c");
}

